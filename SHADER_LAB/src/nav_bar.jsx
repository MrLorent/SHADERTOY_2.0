import createElement from "./Inputs/createElement.js";

export default (shaders_name) => {
    const nav_bar = 
    <nav>
        <ul id="shader_list">
        
        </ul>
    </nav>;

    return nav_bar;
}